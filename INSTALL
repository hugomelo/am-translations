In case you run into problems installing mysql gem. You can be missing this command:
sudo apt-get install libmysqlclient-dev

After that, you can run into problems install generating devise installation files as it requires a js engine.
sudo apt-get install nodejs

for solving that
